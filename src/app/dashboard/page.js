import { Container, Stack, Typography } from "@mui/material"

const page = () => {
  return (
    <Container>
        <Stack spacing={2}>
            <Typography>
                Bienvenido
            </Typography>
        </Stack>
    </Container>
  )
}

export default page